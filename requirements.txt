# parcel-kau-a runtime dependencies.
# Kept ASCII-only on purpose: pip reads this file with the system locale encoding,
# and non-ASCII comments make installation fail on a Traditional Chinese Windows
# (cp950) with UnicodeDecodeError. Reported from a real Windows install, 2026-08-02.
#
# Python >= 3.10 (playwright 1.62 requires it; this project also uses 3.10+ syntax).
requests>=2.34.2
beautifulsoup4>=4.15.0

# Optional, for Shopee SPX only -- the other five couriers work without it:
#   pip install "playwright>=1.62.0" && playwright install chromium
